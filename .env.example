# --- General Configuration ---
LOG_LEVEL="INFO" # Can be DEBUG, INFO, WARNING, ERROR

# --- Blockchain Configuration ---
# Your primary execution wallet private key.
# IMPORTANT: Use a dedicated, isolated hot wallet with limited capital.
EXECUTOR_PRIVATE_KEY="0x..."

# Your RPC endpoint for the target chain (e.g., Infura, Alchemy, or a private node)
ETH_RPC_URL_1="https://mainnet.infura.io/v3/your_infura_key"
# Mempool WebSocket endpoint for tx monitoring
MEMPOOL_WSS_URL="wss://mempool.example/ws"

# --- CEX Adapter Secrets (Example for Binance) ---
BINANCE_API_KEY="your_binance_api_key"
BINANCE_API_SECRET="your_binance_api_secret"

# --- GCP Configuration ---
# Required for deploying to GCP Cloud Run and using Secret Manager
GCP_PROJECT_ID="your-gcp-project-id"
GCP_REGION="us-central1"

# --- Monitoring & Alerting ---
SENTRY_DSN="your_sentry_dsn_url"
