class BridgeAdapter:
    """Placeholder bridge adapter."""

    def transfer(self, token, amount, destination):
        return {
            'token': token,
            'amount': amount,
            'destination': destination,
        }
